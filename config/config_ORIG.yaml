api:
  base_url: "https://api.fda.gov/drug/event.json"
  limit: 100
  max_records: 1000

data:
  raw_path: "data/raw"
  staging_path: "data/staging"

logging:
  log_path: "logs"
  log_level: "INFO"

database:
  db_type: "sqlite"
  sqlite_path: "data/faers_pipeline.db"
